import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:radix_challenge/app/modules/repositories/repositories_controller.dart';
import 'package:radix_challenge/app/app_module.dart';

void main() {
  initModule(AppModule());
  // RepositoriesController repositories;
  //
  setUp(() {
    //     repositories = AppModule.to.get<RepositoriesController>();
  });

  group('RepositoriesController Test', () {
    //   test("First Test", () {
    //     expect(repositories, isInstanceOf<RepositoriesController>());
    //   });

    //   test("Set Value", () {
    //     expect(repositories.value, equals(0));
    //     repositories.increment();
    //     expect(repositories.value, equals(1));
    //   });
  });
}
