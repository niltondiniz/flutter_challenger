import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:radix_challenge/app/modules/repositories/repositories_page.dart';

void main() {
  testWidgets('RepositoriesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RepositoriesPage(title: 'Repositories')));
    //  final titleFinder = find.text('Repositories');
    //  expect(titleFinder, findsOneWidget);
  });
}
