import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:radix_challenge/app/app_module.dart';

void main() {
  initModule(AppModule());
  // RepositoryController repository;
  //
  setUp(() {
    //     repository = AppModule.to.get<RepositoryController>();
  });

  group('RepositoryController Test', () {
    //   test("First Test", () {
    //     expect(repository, isInstanceOf<RepositoryController>());
    //   });

    //   test("Set Value", () {
    //     expect(repository.value, equals(0));
    //     repository.increment();
    //     expect(repository.value, equals(1));
    //   });
  });
}
