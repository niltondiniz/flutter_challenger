import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:radix_challenge/app/modules/repository/repository_page.dart';

void main() {
  testWidgets('RepositoryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RepositoryPage(title: 'Repository')));
    //  final titleFinder = find.text('Repository');
    //  expect(titleFinder, findsOneWidget);
  });
}
