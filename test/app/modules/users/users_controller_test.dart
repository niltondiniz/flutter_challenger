import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:radix_challenge/app/modules/users/users_controller.dart';

void main() {
  //initModule(UsersModule());
  // UsersController users;
  //
  setUp(() {
    //     users = UsersModule.to.get<UsersController>();
  });

  group('UsersController Test', () {
    //   test("First Test", () {
    //     expect(users, isInstanceOf<UsersController>());
    //   });

    //   test("Set Value", () {
    //     expect(users.value, equals(0));
    //     users.increment();
    //     expect(users.value, equals(1));
    //   });
  });
}
